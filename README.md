# alexmelnyk.github.io

run 'npm install'

run 'npm run build' for make a bundle. if not, change path from '/build/app.bundle.js' to 'app.bundle.js'

run 'npm start'

weather API works only over HTTP
